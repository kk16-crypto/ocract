[project]
name = "ocract"
version = "0.1.0"
description = "App for reading text from images and pdfs of documents"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "opencv-python>=4.13.0.92",
    "pyinstaller>=6.20.0",
    "pyside6>=6.11.0",
    "pytesseract>=0.3.13",
]
